# 气体-粒子-激光耦合求解器技术文档
## LaserDPMFoam (OpenFOAM v2506)

---

## 目录
1. [系统概述](#系统概述)
2. [气体动力学模型](#气体动力学模型)
3. [粒子动力学模型](#粒子动力学模型)
4. [激光加热模型](#激光加热模型)
5. [耦合机制详解](#耦合机制详解)
6. [数值求解策略](#数值求解策略)
7. [能量守恒分析](#能量守恒分析)
8. [完整时间推进流程](#完整时间推进流程)
9. [应用案例与配置](#应用案例与配置)

---

## 系统概述

### 1.1 求解器架构

`LaserDPMFoam` 在 OpenFOAM v2506 框架下实现**气体-粒子-激光**耦合：

```
┌──────────────────────────────────────────────────────────┐
│                     LaserDPMFoam                          │
│            (激光-粒子温度-连续相 DPM 耦合)               │
└──────────────────────────────────────────────────────────┘

        ↙              ↓              ↘

   【气体动力学】   【粒子动力学】    【激光加热】
   (Eulerian)      (Lagrangian)      (Lagrangian)
   
   · 速度 Uc        · 位置 x_p        · TEM00 高斯光束
   · 压力 p         · 速度 U_p        · 移动激光头
   · 体积分数 αc    · 温度 T_p        · 解析强度场
   · 湍流量         · 碰撞（可选）    · 不计衰减

        ↖              ↑              ↗

  αc ← 1 − kinematicCloud.theta()
  cloudVolSUSu ← −cloudSU.source()/V
  T_p ← Q_laser(I) − Q_rad − Q_conv
```

### 1.2 耦合类型

**① 气体→粒子（单向信息流）**
- 曳力：`nonSphereDrag`（Haider-Levenspiel，形状系数 φ=0.8）
- 重力：$\vec{F}_g = m_p \vec{g}$
- 压力梯度力：`pressureGradient`

**② 粒子→气体（反馈耦合）**
- 动量源项：$\vec{S}_U = -\text{cloudSU.source()} / V$（显式，面中心）
- 体积占据：$\alpha_c = \max(1 - \alpha_p,\ \alpha_{c,\min})$

**③ 激光→粒子（单向能量输入）**
- 解析 TEM00 高斯强度，每时间步直接计算
- 激光功率经吸收率转化为 $Q_{laser} = \alpha_{laser} \cdot I(x,r) \cdot A_{proj}$
- 当前实现**不计粒子云对激光的衰减**（`laserAttenuationFactor` 恒为 1）

### 1.3 关键物理参数

| 参数 | 符号 | 单位 | 参考值（Stellite 6） | 说明 |
|------|------|------|----------------------|------|
| 激光功率 | $P$ | W | 300 | 总输入功率 |
| 最小光束半径 | $R_0$ | m | 0.75 mm | 焦点处光斑半径 |
| 半角 | $\theta$ | rad | 0.0646 (≈3.7°) | 会聚/发散半角 |
| 焦距 | $x_f$ | m | 8.5 mm | 焦点位置 |
| 焦深 | DOF | m | 5 mm | 平焦区长度 |
| 激光吸收率 | $\alpha_{laser}$ | — | 0.47 | 材料属性 |
| 辐射率 | $\epsilon_p$ | — | 0.0–0.3 | 材料属性 |
| 粒子密度 | $\rho_p$ | kg/m³ | 8380 | Stellite 6 |
| 比热容 | $c_p$ | J/(kg·K) | 421 | Stellite 6 |
| 融化潜热 | $L_f$ | J/kg | 2.92×10⁵ | Stellite 6 |
| 固相线 | $T_{sol}$ | K | 1533 | Stellite 6 |
| 液相线 | $T_{liq}$ | K | 1630 | Stellite 6 |

---

## 气体动力学模型

### 2.1 控制方程

#### 连续性方程
$$\frac{\partial \alpha_c}{\partial t} + \nabla \cdot (\alpha_c \vec{U}_c) = 0$$

其中 $\alpha_c = 1 - \alpha_p$ 为气体体积分数，由粒子云 `theta()` 计算。

#### 动量方程
$$\frac{\partial(\alpha_c \vec{U}_c)}{\partial t} + \nabla \cdot (\alpha_c \vec{U}_c \vec{U}_c) - S_p(\nabla \alpha_c + \nabla \cdot \alpha_c \vec{U}_c,\ \vec{U}_c) + \nabla \cdot \tau_c = \frac{1}{\rho_c} \vec{S}_U$$

其中 $\vec{S}_U$ 为粒子对气体的动量源（显式）：

```cpp
cloudVolSUSu = -cloudSU.source() / mesh.V();   // [N/m³]
cloudSU.source() = Zero;                        // 清零（源项已移至 face-explicit）
```

湍流项采用 `divDevRhoReff`（Reynolds 应力散度含有效粘度）。

#### 压力修正方程（PISO 内循环）
$$\nabla \cdot \left(\alpha_c^f r_{A,Uc}^f \nabla p\right) = \frac{\partial \alpha_c}{\partial t} + \nabla \cdot \left(\alpha_c^f \phi_{HbyA}\right)$$

其中 $r_{A,Uc} = 1/A_{Uc}$ 为动量方程对角系数的倒数。

### 2.2 体积分数更新

```cpp
// 粒子体积分数 → 气体体积分数
alphac = max(1.0 - kinematicCloud.theta(), alphacMin);
alphac.correctBoundaryConditions();
alphacf  = fvc::interpolate(alphac);
alphaPhic = alphacf * phic;
```

**约束：**
- $\alpha_{c,\min} = 1 - \alpha_{p,\max}$，$\alpha_{p,\max}$ 取自 `constantProperties.alphaMax`（参考算例设为 0.9）
- 保证气体连通性，防止单元被粒子完全堵塞

### 2.3 曳力模型

当前算例采用 **Haider-Levenspiel 非球形曳力**（`nonSphereDrag`），适合 Stellite 6 卫星球形粉末（φ=0.8）：

$$C_d = \frac{24}{Re_p}\left(1 + 8.1716 \exp(-4.0655\,\phi)\, Re_p^{0.0964+0.5565\,\phi}\right) + \frac{73.69\,Re_p \exp(-5.0748\,\phi)}{Re_p + 5.378 \exp(6.2122\,\phi)}$$

备选模型（代码中已注释）：Wen-Yu 密集粒子曳力 (`WenYuDrag`)。

### 2.4 粒子作用力汇总

| 力模型 | 代码关键字 | 说明 |
|--------|-----------|------|
| 非球形曳力 | `nonSphereDrag` (φ=0.8) | Haider-Levenspiel 关联式 |
| 重力 | `gravity` | $\vec{F}_g = m_p \vec{g}$ |
| 压力梯度力 | `pressureGradient` | $\vec{F}_p = -V_p \nabla p$ |

---

## 粒子动力学模型

### 3.1 粒子运动方程

#### 位置方程
$$\frac{d\vec{x}_p}{dt} = \vec{U}_p$$

#### 速度方程（Lagrangian 显式 Euler）
$$m_p \frac{d\vec{U}_p}{dt} = \vec{F}_{drag} + m_p \vec{g} + \vec{F}_{pressure\ gradient}$$

速度积分方案由 `integrationSchemes { U Euler; }` 配置。

### 3.2 粒子温度方程（`particleInfo.H`）

#### 能量平衡

$$m_p \frac{dH_p}{dt} = Q_{laser} - Q_{rad} - Q_{conv}$$

其中 $H_p$ 为比焓（含相变潜热，见§3.3），$m_p = \rho_p V_p$。

**激光加热（TEM00 高斯解析式）：**
$$Q_{laser} = \alpha_{laser} \cdot I(x, r) \cdot A_{proj}$$

- $A_{proj} = \frac{\pi d_p^2}{4}$：粒子投影面积
- $I(x,r)$ 的计算见§4

**辐射热损失（Stefan-Boltzmann）：**
$$Q_{rad} = \epsilon_p \sigma_{SB} A_{surf} \left(T_p^4 - T_{amb}^4\right)$$

- $A_{surf} = \pi d_p^2$：粒子表面积
- $\sigma_{SB} = 5.67 \times 10^{-8}$ W/(m²·K⁴)

**对流热损失（Ranz-Marshall 关联式）：**
$$Q_{conv} = h A_{surf} (T_p - T_{amb})$$

$$h = \frac{Nu \cdot k_g}{d_p}, \qquad Nu = 2 + 0.6\, Re_p^{1/2}\, Pr_g^{1/3}$$

$$Re_p = \frac{\rho_c |\vec{U}_c - \vec{U}_p| d_p}{\mu_c}$$

气体热导率 $k_g$ 和普朗特数 $Pr_g$ 由 `particleProperties` 提供（参考值：Ar 气体 $k_g = 0.0158$ W/(m·K)，$Pr_g = 0.741$）。

### 3.3 相变潜热（焓方法）

粒子温度采用**焓方法**处理固-液相变，避免虚假振荡：

$$H_p(T) = \begin{cases}
c_p T & T \leq T_{sol} \quad \text{(固相)}\\
c_p T + L_f \dfrac{T - T_{sol}}{T_{liq} - T_{sol}} & T_{sol} < T < T_{liq} \quad \text{(糊状区)}\\
c_p T + L_f & T \geq T_{liq} \quad \text{(液相)}
\end{cases}$$

时间推进（显式 Euler）：
$$H_p^{n+1} = H_p^n + \frac{Q_{laser} - Q_{rad} - Q_{conv}}{m_p}\,\Delta t$$

再由 $H_p^{n+1}$ 反求 $T_p^{n+1}$：

$$T_p^{n+1} = \begin{cases}
H_p^{n+1} / c_p & H_p^{n+1} \leq H_{sol}\\
\dfrac{H_p^{n+1}\,(T_{liq}-T_{sol}) + L_f\,T_{sol}}{c_p(T_{liq}-T_{sol}) + L_f} & H_{sol} < H_p^{n+1} < H_{liq}\\
(H_p^{n+1} - L_f) / c_p & H_p^{n+1} \geq H_{liq}
\end{cases}$$

### 3.4 数值稳定性检验

每步计算后对显式 Euler 稳定条件进行检验：

$$\tau_{thermal} = \frac{m_p\,c_p}{\beta_{eff}}, \qquad \beta_{eff} = A_{surf}\left(h + 4\epsilon_p\sigma_{SB}T_p^3\right)$$

若 $\Delta t > 2\tau_{thermal}$ 则输出警告（`WarningInFunction`）。

### 3.5 粒子温度存储与 MPI 同步

```cpp
static Map<scalar> T_persistent;   // key = origProc*1e6 + origId
```

- **每时间步**计算所有本地粒子的新温度
- **每时间步**通过 `gatherList / broadcastList` 广播至所有处理器
- **仅在 `writeTime`** 将 "T" 场写入 `lagrangian/<cloudName>/T`
- 处理器间粒子迁移：当前所有者的值始终覆盖旧值

### 3.6 粒子云类型

| 编译宏 | 云类型 | 特性 |
|--------|--------|------|
| 默认（无 MPPIC） | `basicKinematicCollidingCloud` | 粒子-粒子碰撞 |
| `-DMPPIC` | `basicKinematicCloud` | MPPIC 算法，适合稠密流 |

---

## 激光加热模型

### 4.1 TEM00 高斯光束模型（`LaserAttenuation.H`）

#### 强度分布公式

$$I(x, r) = \frac{2P}{\pi R_b(x)^2} \exp\!\left(-\frac{2r^2}{R_b(x)^2}\right)$$

- $x$：沿 `laserDirection` 从 `laserOrigin` 起的轴向距离
- $r$：粒子（或单元中心）到激光轴的径向距离
- $P$：激光总功率 `laserPower` [W]
- $R_b(x)$：有效光束半径，随轴向位置变化

#### 光束半径模型（含焦深）

$$R_b(x) = R_0 + d_{DOF}(x)\,\tan\theta$$

$$d_{DOF}(x) = \max\!\left(0,\ x_f - \frac{DOF}{2} - x\right) + \max\!\left(0,\ x - x_f - \frac{DOF}{2}\right)$$

```
         converging      flat focus zone     diverging
  ──────────────────┬──────────────────┬──────────────────
                    xf - DOF/2         xf + DOF/2
  Rb(x) = R0 + (dist to DOF) * tan(θ)
```

| 参数 | 字典关键字 | 单位 | 说明 |
|------|-----------|------|------|
| $P$ | `laserPower` | W | 总功率 |
| $R_0$ | `beamRadiusMin` | m | 焦点最小光斑半径 |
| $\theta$ | `halfAngle` | rad | 会聚/发散半角 |
| $x_f$ | `focalDistance` | m | 焦点到原点的轴向距离 |
| DOF | `depthOfField` | m | 平焦区（Depth of Field）长度 |

#### 移动激光头

```cpp
const vector laserOrigin = laserInitPos + laserVel * runTime.value();
```

- `laserInitialPosition`：$t=0$ 时的激光原点
- `laserVelocity`：扫描速度向量 [m/s]

#### 静态激光优化

若 `laserVelocity == 0`，`laserIntensity` 场只在第一个时间步计算一次，后续步复用。

### 4.2 激光强度欧拉场

`laserIntensity` [W/m²] 是定义在网格单元中心的 `volScalarField`，仅在 `outputTime` 写入磁盘。

```cpp
// 截断条件（性能优化）：r > 3*Rb 时 I = 0
if (r > 3.0 * Rb)
    laserIntensity[celli] = 0.0;
else
    laserIntensity[celli] = (2.0*P / (pi*sqr(Rb))) * exp(-2.0*sqr(r)/sqr(Rb));
```

### 4.3 粒子加热中的强度计算

`particleInfo.H` 对每个粒子**独立解析计算** $I(x,r)$（不插值 Euler 场），直接读取 `LaserProperties`：

```cpp
const vector relPos  = p.position() - laserOriginNow;
const scalar x_l     = relPos & laserDir;          // 轴向距离
const scalar r_l     = mag(relPos - x_l*laserDir); // 径向距离
// ... 计算 Rb, I_laser ...
const scalar Q_laser = alpha_laser * I_laser * A_proj;
```

### 4.4 激光衰减（当前状态）

`laserAttenuationFactor` 场在 `createFields.H` 中初始化为 1，**当前主循环中不更新**。即：

> **当前实现为单向激光-粒子耦合：激光不受粒子云遮挡影响。**

若需实现 Beer-Lambert 衰减，需在 `LaserAttenuation.H` 中添加沿光路的粒子消光积分。

---

## 耦合机制详解

### 5.1 耦合矩阵

| 方向 | 机制 | 实现位置 | 当前状态 |
|------|------|---------|---------|
| 气体→粒子 | 曳力、重力、压力梯度 | `kinematicCloud.evolve()` | ✅ 双向 |
| 粒子→气体 | 动量源 `cloudVolSUSu`、体积分数 `alphac` | `LaserDPMFoam.C` | ✅ 双向 |
| 激光→粒子 | Gaussian 加热 $Q_{laser}$ | `particleInfo.H` | ✅ 单向 |
| 粒子→激光 | Beer-Lambert 衰减 | 未实现 | ⚠️ 恒为1 |

### 5.2 气体-粒子耦合

#### 粒子→气体

```cpp
// 1. 粒子云演化（Lagrangian 求解）
kinematicCloud.evolve();

// 2. 更新气体体积分数
alphac = max(1.0 - kinematicCloud.theta(), alphacMin);
alphacf  = fvc::interpolate(alphac);
alphaPhic = alphacf * phic;

// 3. 提取动量源项（面显式策略）
fvVectorMatrix cloudSU = kinematicCloud.SU(Uc);
cloudVolSUSu.primitiveFieldRef() = -cloudSU.source() / mesh.V();
cloudVolSUSu.correctBoundaryConditions();
cloudSU.source() = Zero;   // 隐式部分已移至面，source 清零

// 4. 注入动量方程
fvVectorMatrix UcEqn(
    fvm::ddt(alphac, Uc) + fvm::div(alphaPhic, Uc)
  - fvm::Sp(fvc::ddt(alphac) + fvc::div(alphaPhic), Uc)
  + continuousPhaseTurbulence->divDevRhoReff(Uc)
 ==
    (1.0/rhoc) * cloudSU
);
```

#### 气体→粒子（通过 ParcelCloud 力模型）

```cpp
// 内部循环（每个粒子，kinematicCloud.evolve() 内部）
forAllConstIter(basicKinematicTypeCloud, kinematicCloud, iter)
{
    const auto& p = iter();
    label cellI = p.cell();

    // nonSphereDrag: 从 Uc[cellI], rhoc[cellI], muc[cellI] 计算曳力
    // gravity: g
    // pressureGradient: ∇p[cellI]
}
```

### 5.3 激光-粒子耦合（`particleInfo.H`）

```
每个 CFD 时间步
│
├── 读 LaserProperties（每步，支持移动激光头）
├── 读 particleProperties（仅首步，static）
│
└── forAllConstIter(kinematicCloud, iter)
    ├── 计算 I(x_p, r_p)    ← 解析 Gaussian，不读 Euler 场
    ├── 计算 Q_laser, Q_rad, Q_conv
    ├── 焓法更新 T_p（含潜热）
    ├── 写入 T_persistent[key]
    └── 稳定性检验
│
├── MPI gatherList / broadcastList    ← 粒子迁移安全
└── (writeTime) 写 lagrangian/kinematicCloud/T
```

### 5.4 时间步内执行顺序

```
++runTime

continuousPhaseTransport.correct()      // 气体物性
muc = rhoc * nu

kinematicCloud.evolve()                 // Lagrangian：粒子位置/速度/碰撞
alphac  ← 1 − theta()                  // 体积分数更新
cloudVolSUSu ← −cloudSU.source()/V     // 动量源

while (pimple.loop())
│   UcEqn (含 cloudSU、cloudVolSUSu)    // 动量预测
│   while (pimple.correct()) pEqn      // PISO 压力修正
└── continuousPhaseTurbulence->correct()

runTime.write()                         // 写气体场 + Lagrangian 场

#include "LaserAttenuation.H"           // 计算/更新 laserIntensity Euler 场
#include "particleInfo.H"              // 计算粒子温度（每步），写 T（仅 writeTime）
```

---

## 数值求解策略

### 6.1 时间离散化

**气体**：一阶隐式 Euler（`fvm::ddt`）：
$$\frac{u^{n+1} - u^n}{\Delta t} = RHS^{n+1}$$

**粒子速度**：显式 Euler（`integrationSchemes { U Euler; }`）

**粒子温度**：显式 Euler（单步，`particleInfo.H`，每个 CFD $\Delta t$ 执行一次）

### 6.2 空间离散化

| 项 | 方案 | 说明 |
|----|------|------|
| $\frac{\partial}{\partial t}$ | `Euler` | 一阶时间推进 |
| $\nabla \cdot (\alpha \vec{U}\vec{U})$ | `Gauss limitedLinear` | 有界对流 |
| $\nabla \cdot \tau$ | `divDevRhoReff` | 湍流应力散度 |
| $\nabla^2 p$ | `Gauss linear` | 压力扩散项 |
| 面插值 | `linear` | 默认线性插值 |

### 6.3 PIMPLE 循环

```
while (runTime.run())
{
    ++runTime;
    
    kinematicCloud.evolve();           // Lagrangian
    alphac, cloudVolSUSu 更新

    while (pimple.loop())
    {
        UcEqn.solve()                  // 动量预测（可选）
        
        while (pimple.correctNonOrthogonal())
        {
            pEqn.solve()               // 压力修正
        }
        
        // 速度、通量修正
        phic = phiHbyA - pEqn.flux()/alphacf;
        Uc = HbyA + rAUc * fvc::reconstruct(...);

        continuousPhaseTurbulence->correct();
    }
}
```

### 6.4 Courant 数计算

$$Co = \frac{1}{2} \frac{\Delta t}{V_{cell}} \sum_{faces} |\phi_c|$$

由 `CourantNo.H` 计算，输出均值和最大值，控制自适应时间步（`maxCo = 0.8`，参考算例）。

### 6.5 自适应时间步

时间步由 `setDeltaT.H` 根据 Courant 数调节，受 `maxCo` 和 `maxDeltaT` 双重约束。

---

## 能量守恒分析

### 7.1 全局能量平衡

```
INPUT  :  Q_laser = α_laser · I(x,r) · A_proj    （激光吸收功率）

BALANCE:  ΔH_p = ∫(Q_laser - Q_rad - Q_conv) dt  （粒子焓变，含潜热）

OUTPUT :  Q_rad   = ε σ_SB A_surf (T_p⁴ - T_amb⁴) （辐射损失）
          Q_conv  = h A_surf (T_p - T_amb)          （对流损失）

守恒: INPUT = BALANCE + OUTPUT
注：当前气体无温度方程，Q_conv 不流入气体场
```

### 7.2 单粒子能量方程

```cpp
// 焓方法（particleInfo.H 核心逻辑）
const scalar H_old = enthalpyFromT(T_curr);
const scalar H_new = H_old + (Q_laser - Q_rad - Q_conv) * dt / m;
T_curr = tempFromEnthalpy(H_new);   // 含糊状区插值
T_persistent.set(key, T_curr);
```

### 7.3 气体能量方程（当前未启用）

气体温度场当前**未求解**，仅有速度和压力。若需启用：
$$\rho_c c_{p,c} \alpha_c \left(\frac{\partial T_c}{\partial t} + \vec{U}_c \cdot \nabla T_c\right) = \nabla \cdot (k_c \nabla T_c) + Q_{particle \to gas}$$

其中 $Q_{particle \to gas} = \sum_p Q_{conv}$ 为粒子对气体的换热源项。

---

## 完整时间推进流程

### 8.1 主循环伪代码（基于 `LaserDPMFoam.C`）

```cpp
while (runTime.run())
{
    #include "readTimeControls.H"
    #include "CourantNo.H"      // 计算 Co
    #include "setDeltaT.H"      // 自适应 Δt

    ++runTime;
    Info<< "Time = " << runTime.timeName() << endl;

    // ===== 步骤 1：气体物性更新 =====
    continuousPhaseTransport.correct();
    muc = rhoc * continuousPhaseTransport.nu();

    // ===== 步骤 2：粒子云演化（Lagrangian） =====
    // - nonSphereDrag/gravity/pressureGradient 作用于每个粒子
    // - 粒子碰撞（basicKinematicCollidingCloud）
    // - 注入器：patchInjection，按 parcelBasisType fixed
    kinematicCloud.evolve();

    // ===== 步骤 3：体积分数更新（粒子→气体） =====
    alphac = max(1.0 - kinematicCloud.theta(), alphacMin);
    alphac.correctBoundaryConditions();
    alphacf  = fvc::interpolate(alphac);
    alphaPhic = alphacf * phic;

    // ===== 步骤 4：粒子动量源项（面显式） =====
    fvVectorMatrix cloudSU = kinematicCloud.SU(Uc);
    cloudVolSUSu.primitiveFieldRef() = -cloudSU.source() / mesh.V();
    cloudVolSUSu.correctBoundaryConditions();
    cloudSU.source() = Zero;

    // ===== 步骤 5：气体 PIMPLE 循环 =====
    while (pimple.loop())
    {
        #include "UcEqn.H"    // 动量方程（含 cloudSU, cloudVolSUSu, 重力）

        while (pimple.correct())
        {
            #include "pEqn.H"  // PISO 压力修正
        }

        if (pimple.turbCorr())
            continuousPhaseTurbulence->correct();
    }

    // ===== 步骤 6：场写出 =====
    runTime.write();

    // ===== 步骤 7：激光场计算（每步） =====
    // LaserAttenuation.H: 计算 laserIntensity[cell]（静态激光只算一次）
    // particleInfo.H: 计算/更新粒子温度 T_persistent（每步），
    //                  仅 writeTime 时写 lagrangian/kinematicCloud/T
    #include "LaserAttenuation.H"
    #include "particleInfo.H"

    runTime.printExecutionTime(Info);
}
```

### 8.2 关键设计决策

**① 源项分配策略（面显式）**

```
cloudVolSUSu = -cloudSU.source() / mesh.V()   → 进入 UcEqn 右端 phicForces
cloudSU.source() = Zero                        → 矩阵中隐式部分置零
```

动量方程右端重组：
```cpp
surfaceScalarField phicForces =
    fvc::flux(rAUc * cloudVolSUSu / rhoc) + rAUcf*(g & mesh.Sf());
```

**② 粒子温度不依赖 Euler 激光场**

`particleInfo.H` 对每个粒子**重新解析计算** $I(x_p, r_p)$，与 `laserIntensity` Euler 场独立，避免插值误差，也无需等待 `runTime.write()`。

**③ 激光强度场的静态优化**

```cpp
static bool laserIntensityComputed = false;
if (laserIsMoving || !laserIntensityComputed)
{
    // 重算
    laserIntensityComputed = true;
}
else
{
    Info<< "static laser - reusing pre-computed field" << endl;
}
```

---

## 应用案例与配置

### 9.1 参考算例（`00test/Laser_3D_refer`）

**工况：激光粉末沉积（DED，Stellite 6 合金 + Ar 保护气）**

```
激光功率:  300 W
最小光斑:  R0 = 0.75 mm
半角:      θ = 0.0646 rad (3.7°)
焦距:      8.5 mm
焦深:      5 mm
激光方向:  (1 0 0)（沿 X 轴射入）
注射补片:  powderInlet，patchInjection
粒子形状:  非球形（φ = 0.8，卫星粉末）
端时间:    0.5 s，Δt = 2.5×10⁻⁵ s
输出间隔:  1 ms
```

### 9.2 配置文件示例

#### `constant/LaserProperties`

```cpp
// 移动激光头（可选）
laserInitialPosition (0.0 0.0 0.0);  // t=0 时原点 [m]
laserVelocity        (0.0 0.0 0.0);  // 扫描速度 [m/s]

// 传播方向与原点
laserDirection  (1 0 0);              // 归一化（代码自动处理）
laserOrigin     (0.0 0.0 0.0);        // 轴原点

// 功率
laserPower      300.0;                // [W]

// 光束半径模型
beamRadiusMin   7.5e-4;               // R0 [m]
halfAngle       0.06458;              // θ [rad]
focalDistance   8.5e-3;               // xf [m]
depthOfField    5.0e-3;               // DOF [m]
```

#### `constant/particleProperties`（Stellite 6）

```cpp
sigma       5.67e-8;    // Stefan-Boltzmann [W/(m²·K⁴)]
T_amb       300.0;      // 环境温度 [K]
T_init      300.0;      // 初始粒子温度 [K]

alpha_laser 0.47;       // 激光吸收率 [-]
epsilon     0.0;        // 热辐射率 [-]（可按实际材料修改）
cp          421.0;      // 比热容 [J/(kg·K)]
rhoP        8380.0;     // 密度 [kg/m³]

L_f         2.92e5;     // 融化潜热 [J/kg]
T_sol       1533.0;     // 固相线 [K]
T_liq       1630.0;     // 液相线 [K]

k_g         0.01580;    // 气体热导率 [W/(m·K)]（Ar）
Pr_g        0.741;      // 气体 Prandtl 数 [-]（Ar）
```

#### `system/controlDict`

```cpp
application     LaserDPMFoam;

startTime       0.0;
endTime         0.5;
deltaT          2.5e-5;

writeControl    runTime;
writeInterval   0.001;     // 1 ms 输出一次

runTimeModifiable yes;
maxCo           0.8;
```

### 9.3 调试建议

**① 验证激光强度场**
```cpp
// 在 LaserAttenuation.H 执行后查看
Info<< "Peak laser intensity = " << gMax(laserIntensity) << " W/m^2" << endl;
// 静止激光仅在 t=0 计算，后续打印 "reusing pre-computed field"
```

**② 粒子温度监控（求解器日志）**

每时间步 `particleInfo.H` 输出：
```
particleInfo [t=0.001]:  N=5000  heated=432  T_max=1523 K  dT_max=12.3 K  pos(dT_max)=(0.012 0 0)
```

**③ 后处理粒子温度**
```python
from pathlib import Path
import numpy as np

for t in sorted(Path('.').glob('[0-9]*')):
    T_file = t / 'lagrangian/kinematicCloud/T'
    if T_file.exists():
        data = open(T_file).read().split()
        # 找到数据起始位置后读取
        print(f"t={t.name}: T_max={max(float(v) for v in data if v.replace('.','').isdigit()):.0f} K")
```

**④ 检查粒子碰撞是否启用**

确认编译时未定义 `-DMPPIC`（`Make/options` 无 `-DMPPIC`），则使用 `basicKinematicCollidingCloud`。

---

## 总结与扩展方向

### 已实现功能
- 气体-粒子双向动量耦合（`cloudVolSUSu` 面显式，`alphac` 体积分数反馈）
- TEM00 Gaussian 光束（含焦深、会聚/发散）
- 移动激光头支持（`laserInitialPosition + laserVelocity*t`）
- 粒子温度追踪（焓方法，含固液相变潜热）
- Ranz-Marshall 对流换热 + Stefan-Boltzmann 辐射
- MPI 并行（粒子迁移安全的 `T_persistent` 全局同步）
- 显式 Euler 稳定性自动检验
- 静态激光强度场缓存优化

### 当前限制
- 激光衰减未实现（`laserAttenuationFactor` 恒为 1）
- 无气体温度求解（$Q_{conv}$ 不反馈到气体）
- 粒子温度显式 Euler 积分（时间步受 $\tau_{thermal}$ 约束）
- 辐射仅为粒子↔环境（无粒子间辐射交换）

### 可扩展方向
1. **激光衰减**：在 `LaserAttenuation.H` 中实现 Beer-Lambert 积分，更新 `laserAttenuationFactor`，并在 `particleInfo.H` 中将 $I$ 乘以对应单元的衰减因子
2. **气体温度**：添加 $T_c$ 方程，并将 $Q_{conv}$ 注入气体能量源
3. **隐式粒子温度积分**：将 ODE 改为隐式（$T^{n+1}$ 线性化辐射项），消除稳定性约束
4. **动网格**：集成 `DPMDyMFoam` 的 `correctPhic.H` 和 `createUcf.H`，支持熔池表面变形

---

**文档版本：3.0**
**日期：2026-05-25**
**求解器：LaserDPMFoam (OpenFOAM v2506)**
