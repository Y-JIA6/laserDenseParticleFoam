# 从positions文件确定粒子全局坐标的详细算法

## 问题：如何从positions文件的7个数据计算出粒子的xyz全局坐标？

---

## 1. positions文件数据结构回顾

```cpp
// positions文件示例（以第一个粒子为例）
(0.444429 0.324133 0.0970447 0.134393) 5173 14461 2
 └──────── λ (重心坐标) ────────┘  │     │     │
                                celli  face  pt
```

**7个数据的含义：**
1. `λ0 = 0.444429` - 重心坐标第1分量（指向单元中心）
2. `λ1 = 0.324133` - 重心坐标第2分量（指向面顶点1）
3. `λ2 = 0.0970447` - 重心坐标第3分量（指向面顶点2）
4. `λ3 = 0.134393` - 重心坐标第4分量（指向面顶点3）
5. `celli = 5173` - 六面体单元编号
6. `tetFacei = 14461` - 该单元的哪个面
7. `tetPti = 2` - 面上的哪个点（相对索引）

---

## 2. 坐标转换的5个步骤

### 步骤1：确定六面体单元

```cpp
label celli = 5173;  // 从positions文件读取

// 获取单元中心坐标
const vectorField& cellCentres = mesh.cellCentres();
vector C = cellCentres[celli];  // 这是四面体的第1个顶点

Info << "Cell " << celli << " centre: " << C << endl;
// 输出示例: Cell 5173 centre: (0.015 0.002 0.0)
```

### 步骤2：确定四面体所在的面

```cpp
label tetFacei = 14461;  // 从positions文件读取

// 获取面的顶点索引列表
const faceList& faces = mesh.faces();
const face& f = faces[tetFacei];  // 面是一个顶点索引列表

Info << "Face " << tetFacei << " has " << f.size() << " vertices: " << f << endl;
// 输出示例: Face 14461 has 4 vertices: (1234 1235 1567 1566)
//                                      └─ 四个顶点的全局索引
```

### 步骤3：确定四面体的基准点

OpenFOAM使用面的"基准点"（base point）来定义四面体分解：

```cpp
label tetPti = 2;  // 从positions文件读取（相对索引，相对于基准点）

// 获取面的基准点索引（通常是0，但某些面可能不同）
const labelList& tetBasePtIs = mesh.tetBasePtIs();
label baseI = max(0, tetBasePtIs[tetFacei]);  // 面的基准点在面顶点列表中的位置

Info << "Face base point index in face: " << baseI << endl;
// 输出示例: Face base point index: 0
```

### 步骤4：构建四面体的3个面顶点

四面体由以下4个点组成：
1. **单元中心** C（已获取）
2. **面基准点** v0
3. **面顶点1** v1（基准点后第tetPti个点）
4. **面顶点2** v2（基准点后第tetPti+1个点）

```cpp
// 获取网格所有顶点的坐标
const pointField& points = mesh.points();

// 构建三角形的3个顶点索引（相对于面的基准点）
// tetPti定义了三角形在面上的位置
label faceVertexIndex0 = baseI;                           // 基准点
label faceVertexIndex1 = (baseI + tetPti) % f.size();    // 第tetPti个点
label faceVertexIndex2 = (baseI + tetPti + 1) % f.size(); // 第tetPti+1个点

// 获取这3个顶点在网格中的全局索引
label globalVertexIndex0 = f[faceVertexIndex0];
label globalVertexIndex1 = f[faceVertexIndex1];
label globalVertexIndex2 = f[faceVertexIndex2];

// 获取3个顶点的全局坐标
vector v0 = points[globalVertexIndex0];  // 四面体第2个顶点
vector v1 = points[globalVertexIndex1];  // 四面体第3个顶点
vector v2 = points[globalVertexIndex2];  // 四面体第4个顶点

Info << "Tet vertices:" << endl
     << "  v0 (base): " << v0 << endl
     << "  v1 (base+" << tetPti << "): " << v1 << endl
     << "  v2 (base+" << tetPti+1 << "): " << v2 << endl;
```

**关键理解：tetPti的作用**

```
假设面有4个顶点（四边形）: [A, B, C, D]
基准点baseI = 0，所以基准点是A

tetPti = 0:  使用三角形 (A, B, C)
tetPti = 1:  使用三角形 (A, C, D)
tetPti = 2:  使用三角形 (A, D, E) [如果面有5个顶点]

对于我们的例子tetPti=2:
- v0 = f[0] = A (基准点)
- v1 = f[2] = C (基准点后第2个)
- v2 = f[3] = D (基准点后第3个)
```

### 步骤5：应用重心坐标公式计算全局位置

现在我们有了四面体的4个顶点和重心坐标，可以计算粒子位置：

```cpp
// 从positions文件读取的重心坐标
scalar lambda0 = 0.444429;
scalar lambda1 = 0.324133;
scalar lambda2 = 0.0970447;
scalar lambda3 = 0.134393;

// 重心坐标公式：p = λ0·C + λ1·v0 + λ2·v1 + λ3·v2
vector particlePosition = 
    lambda0 * C  +   // 指向单元中心的权重
    lambda1 * v0 +   // 指向面基准点的权重
    lambda2 * v1 +   // 指向面顶点1的权重
    lambda3 * v2;    // 指向面顶点2的权重

Info << "Particle global position: " << particlePosition << endl;
// 输出示例: Particle global position: (0.0123 0.0045 0.00067)
```

---

## 3. 完整代码示例

### 方法A：手动计算（教学用）

```cpp
// 从positions文件读取数据
scalar lambda0 = 0.444429;
scalar lambda1 = 0.324133;
scalar lambda2 = 0.0970447;
scalar lambda3 = 0.134393;
label celli = 5173;
label tetFacei = 14461;
label tetPti = 2;

// 步骤1：获取单元中心
vector C = mesh.cellCentres()[celli];

// 步骤2-4：获取四面体的3个面顶点
const face& f = mesh.faces()[tetFacei];
const pointField& pts = mesh.points();

// 确定基准点
label baseI = max(0, mesh.tetBasePtIs()[tetFacei]);

// 构建三角形顶点索引（模运算处理循环）
label idx0 = baseI;
label idx1 = (baseI + tetPti) % f.size();
label idx2 = (baseI + tetPti + 1) % f.size();

// 获取顶点坐标
vector v0 = pts[f[idx0]];
vector v1 = pts[f[idx1]];
vector v2 = pts[f[idx2]];

// 步骤5：计算粒子位置
vector position = lambda0*C + lambda1*v0 + lambda2*v1 + lambda3*v2;

Info << "Calculated particle position: " << position << endl;
```

### 方法B：使用OpenFOAM内置方法（推荐）

```cpp
// 这是OpenFOAM实际使用的方法
passiveParticleCloud particles(mesh, "cloud");

forAllConstIter(passiveParticleCloud, particles, iter)
{
    const passiveParticle& p = iter();
    
    // 内部调用 particle::position()
    // 该方法自动完成上述所有步骤
    vector pos = p.position();
    
    Info << "Particle position: " << pos << endl;
    
    // 也可以查看原始数据
    Info << "  coordinates: " << p.coordinates() << endl
         << "  celli: " << p.cell() << endl
         << "  tetFacei: " << p.tetFace() << endl
         << "  tetPti: " << p.tetPt() << endl;
}
```

---

## 4. OpenFOAM源代码实现

### particleI.H 中的实现

```cpp
// 文件: basic/particle/particleI.H

// 获取四面体的4个顶点坐标
inline void Foam::particle::stationaryTetGeometry
(
    vector& centre,
    vector& base,
    vector& vertex1,
    vector& vertex2
) const
{
    // 获取三角形的3个顶点索引
    const triFace triIs(currentTetIndices().faceTriIs(mesh_));
    
    // 获取网格数据
    const vectorField& ccs = mesh_.cellCentres();
    const pointField& pts = mesh_.points();

    // 四面体的4个顶点
    centre = ccs[celli_];        // 单元中心
    base = pts[triIs[0]];        // 面基准点
    vertex1 = pts[triIs[1]];     // 面顶点1
    vertex2 = pts[triIs[2]];     // 面顶点2
}

// 构建四面体变换矩阵
inline Foam::barycentricTensor Foam::particle::stationaryTetTransform() const
{
    vector centre, base, vertex1, vertex2;
    stationaryTetGeometry(centre, base, vertex1, vertex2);

    // 返回包含4个顶点的张量
    return barycentricTensor(centre, base, vertex1, vertex2);
}
```

### particle.C 中的position()实现

```cpp
// 文件: basic/particle/particle.C

Foam::vector Foam::particle::position() const
{
    // 获取四面体变换矩阵（包含4个顶点）
    const barycentricTensor A = currentTetTransform();
    
    // 应用重心坐标变换
    // coordinates_[0-3] 是从positions文件读取的λ0-λ3
    // A.a(), A.b(), A.c(), A.d() 是四面体的4个顶点
    return 
        coordinates_[0] * A.a() +   // λ0 * 单元中心
        coordinates_[1] * A.b() +   // λ1 * 基准点
        coordinates_[2] * A.c() +   // λ2 * 顶点1
        coordinates_[3] * A.d();    // λ3 * 顶点2
}
```

---

## 5. 数学公式总结

### 重心坐标定义

对于四面体顶点 $\mathbf{v}_0, \mathbf{v}_1, \mathbf{v}_2, \mathbf{v}_3$，任意点 $\mathbf{p}$ 可以表示为：

$$
\mathbf{p} = \lambda_0 \mathbf{v}_0 + \lambda_1 \mathbf{v}_1 + \lambda_2 \mathbf{v}_2 + \lambda_3 \mathbf{v}_3
$$

约束条件：
$$
\lambda_0 + \lambda_1 + \lambda_2 + \lambda_3 = 1
$$

点在四面体内部当且仅当：
$$
0 \leq \lambda_i \leq 1, \quad \forall i \in \{0,1,2,3\}
$$

### OpenFOAM的顶点顺序

```
v0 = C  = 单元中心坐标
v1 = p0 = 面基准点坐标
v2 = p1 = 面顶点1坐标  (基准点 + tetPti)
v3 = p2 = 面顶点2坐标  (基准点 + tetPti + 1)
```

因此：
$$
\mathbf{p} = \lambda_0 \mathbf{C} + \lambda_1 \mathbf{p}_0 + \lambda_2 \mathbf{p}_1 + \lambda_3 \mathbf{p}_2
$$

---

## 6. 具体数值示例

### 输入数据（从positions文件）

```
(0.444429 0.324133 0.0970447 0.134393) 5173 14461 2
```

### 中间计算（假设值）

```cpp
// 步骤1：单元中心
C = (0.015, 0.002, 0.0)

// 步骤2-4：获取面和顶点
Face 14461 = [1234, 1235, 1567, 1566]  // 4个顶点索引
baseI = 0
tetPti = 2

// 三角形顶点索引
triIs[0] = f[0] = 1234    (基准点)
triIs[1] = f[2] = 1567    (基准点 + tetPti)
triIs[2] = f[3] = 1566    (基准点 + tetPti + 1)

// 顶点坐标（从mesh.points()获取）
v0 = points[1234] = (0.0145, 0.0018, -0.0005)
v1 = points[1567] = (0.0155, 0.0025, -0.0005)
v2 = points[1566] = (0.0158, 0.0022, -0.0005)
```

### 最终计算

```cpp
position = 0.444429 * (0.015, 0.002, 0.0)
         + 0.324133 * (0.0145, 0.0018, -0.0005)
         + 0.0970447 * (0.0155, 0.0025, -0.0005)
         + 0.134393 * (0.0158, 0.0022, -0.0005)

// 分量计算
x = 0.444429*0.015 + 0.324133*0.0145 + 0.0970447*0.0155 + 0.134393*0.0158
  = 0.006666 + 0.004700 + 0.001504 + 0.002123
  = 0.014993 m

y = 0.444429*0.002 + 0.324133*0.0018 + 0.0970447*0.0025 + 0.134393*0.0022
  = 0.000889 + 0.000583 + 0.000243 + 0.000296
  = 0.002011 m

z = 0.444429*0.0 + 0.324133*(-0.0005) + 0.0970447*(-0.0005) + 0.134393*(-0.0005)
  = 0.0 - 0.000162 - 0.000049 - 0.000067
  = -0.000278 m

// 最终结果
position = (0.014993, 0.002011, -0.000278) m
```

### 验证约束

```cpp
λ0 + λ1 + λ2 + λ3 = 0.444429 + 0.324133 + 0.0970447 + 0.134393
                   = 1.0 ✓

所有λi ∈ [0,1] ✓  （粒子在四面体内部）
```

---

## 7. 可视化示意图

```
        v2 (p2)
         ●
        /│\
       / │ \
      /  │  \          粒子位置p：
     /   │   \         p = 0.444·C + 0.324·v1 + 0.097·v2 + 0.134·v3
    /    │    \
   /     ●p    \       最靠近单元中心C (λ0=0.444最大)
  /     / \     \      其次靠近基准点v1 (λ1=0.324)
 /     /   \     \
v1───────────────v3
(p0)           (p1)
      \    |    /
       \   |   /
        \  |  /
         \ | /
          \|/
           ●
           C (单元中心)
```

---

## 8. 常见问题

### Q1: 为什么λ0指向单元中心而不是顶点？

**A:** OpenFOAM的四面体分解策略是"单元中心+面三角形"，这样可以：
- 保证所有四面体共享单元中心
- 简化四面体构建
- 便于在四面体间切换（穿过面时）

### Q2: tetPti为什么是相对索引？

**A:** tetPti是相对于面基准点的索引，这样：
- 与面的方向无关
- 简化并行计算（不同处理器可能有不同的面方向）
- 保持一致的四面体方向

### Q3: 如何验证计算是否正确？

```cpp
// 方法1：验证重心坐标和
scalar sum = lambda0 + lambda1 + lambda2 + lambda3;
if (mag(sum - 1.0) > 1e-10)
{
    FatalError << "Invalid barycentric coordinates!" << abort(FatalError);
}

// 方法2：验证点在四面体内
if (lambda0 < 0 || lambda1 < 0 || lambda2 < 0 || lambda3 < 0)
{
    Warning << "Particle outside tetrahedron!" << endl;
}

// 方法3：对比OpenFOAM计算的结果
vector posCalc = lambda0*C + lambda1*v0 + lambda2*v1 + lambda3*v2;
vector posOF = particle.position();
if (mag(posCalc - posOF) > 1e-10)
{
    FatalError << "Position mismatch!" << abort(FatalError);
}
```

---

## 9. 实用工具函数

### 完整的坐标转换函数

```cpp
// 工具函数：从positions数据计算粒子位置
vector calculateParticlePosition
(
    const polyMesh& mesh,
    const barycentric& coords,  // (λ0, λ1, λ2, λ3)
    const label celli,
    const label tetFacei,
    const label tetPti
)
{
    // 单元中心
    const vector& C = mesh.cellCentres()[celli];
    
    // 面和点
    const face& f = mesh.faces()[tetFacei];
    const pointField& pts = mesh.points();
    
    // 基准点
    const label baseI = max(0, mesh.tetBasePtIs()[tetFacei]);
    
    // 构建三角形
    const label size = f.size();
    const label i0 = baseI;
    const label i1 = (baseI + tetPti) % size;
    const label i2 = (baseI + tetPti + 1) % size;
    
    // 顶点坐标
    const vector& v0 = pts[f[i0]];
    const vector& v1 = pts[f[i1]];
    const vector& v2 = pts[f[i2]];
    
    // 重心坐标变换
    return coords[0]*C + coords[1]*v0 + coords[2]*v1 + coords[3]*v2;
}

// 使用示例
barycentric coords(0.444429, 0.324133, 0.0970447, 0.134393);
vector pos = calculateParticlePosition(mesh, coords, 5173, 14461, 2);
Info << "Particle at: " << pos << endl;
```

---

## 10. 总结流程图

```
positions文件
    ↓
(λ0 λ1 λ2 λ3) celli tetFacei tetPti
    ↓
    ├─→ celli ──────────→ 获取单元中心 C
    │
    ├─→ tetFacei ───────→ 获取面 f = [v_i, v_j, v_k, v_l]
    │                        ↓
    │                     baseI = tetBasePtIs[tetFacei]
    │                        ↓
    └─→ tetPti ─────────→ 构建三角形索引
                            idx0 = baseI
                            idx1 = (baseI + tetPti) % f.size()
                            idx2 = (baseI + tetPti + 1) % f.size()
                                ↓
                          获取顶点坐标
                            v0 = points[f[idx0]]
                            v1 = points[f[idx1]]
                            v2 = points[f[idx2]]
                                ↓
                          重心坐标变换
                            p = λ0·C + λ1·v0 + λ2·v1 + λ3·v2
                                ↓
                          全局xyz坐标
```

---

## 附录：tetIndices类说明

```cpp
// OpenFOAM使用tetIndices类封装四面体信息
class tetIndices
{
    label celli_;     // 单元编号
    label facei_;     // 面编号
    label tetPti_;    // 四面体点编号
    
    // 获取三角形的3个顶点索引
    triFace faceTriIs(const polyMesh& mesh) const
    {
        const face& f = mesh.faces()[facei_];
        const label baseI = max(0, mesh.tetBasePtIs()[facei_]);
        
        label i0 = baseI;
        label i1 = (baseI + tetPti_) % f.size();
        label i2 = (baseI + tetPti_ + 1) % f.size();
        
        return triFace(f[i0], f[i1], f[i2]);
    }
    
    // 获取三角形对象（包含坐标）
    triPointRef faceTri(const polyMesh& mesh) const
    {
        const triFace tri = faceTriIs(mesh);
        const pointField& pts = mesh.points();
        
        return triPointRef(pts[tri[0]], pts[tri[1]], pts[tri[2]]);
    }
};
```

---

**文档版本**: 1.0  
**日期**: 2024-12-22  
**作者**: Technical Documentation
